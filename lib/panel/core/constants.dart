// Core constants
const double MIN_NODE_SIZE = 10;
const double DEFAULT_NODE_WIDTH = 220;
const double DEFAULT_NODE_HEIGHT = 120;
const double GRID_SIZE = 30.0;

// Debounce duration for history save
const Duration HISTORY_SAVE_DEBOUNCE = Duration(milliseconds: 800);
